# Migration `20210816152609-champions`

This migration has been generated by GiovanniCrabi at 8/16/2021, 12:26:09 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "ChampionsOnBubbles" (
    "championId" INTEGER NOT NULL,
    "bubbleId" INTEGER NOT NULL,
    "assignedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "assignedBy" TEXT NOT NULL,

    PRIMARY KEY ("championId","bubbleId")
)

ALTER TABLE "ChampionsOnBubbles" ADD FOREIGN KEY("championId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "ChampionsOnBubbles" ADD FOREIGN KEY("bubbleId")REFERENCES "Bubble"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210530223518-state-label..20210816152609-champions
--- datamodel.dml
+++ datamodel.dml
@@ -2,38 +2,51 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model Bubble {
-  id          Int       @id @default(autoincrement())
-  createdAt   DateTime  @default(now())
+  id          Int                  @id @default(autoincrement())
+  createdAt   DateTime             @default(now())
   title       String
   description String
   content     String
-  author      User?     @relation(fields: [authorId], references: [id])
+  author      User?                @relation(fields: [authorId], references: [id])
   authorId    Int?
   labels      Label[]
+  champions   ChampionsOnBubbles[]
   comments    Comment[]
   likes       Like[]
 }
 model User {
-  id        Int       @id @default(autoincrement())
-  email     String    @unique
-  name      String?
-  avatarUrl String
-  bubbles   Bubble[]
-  Comment   Comment[]
-  Like      Like[]
+  id                  Int                  @id @default(autoincrement())
+  email               String               @unique
+  name                String?
+  avatarUrl           String
+  bubbles             Bubble[]
+  Comment             Comment[]
+  Like                Like[]
+  bubblesChampionShip ChampionsOnBubbles[]
 }
+model ChampionsOnBubbles {
+  champion   User     @relation(fields: [championId], references: [id])
+  championId Int
+  bubble     Bubble   @relation(fields: [bubbleId], references: [id])
+  bubbleId   Int // relation scalar field (used in the `@relation` attribute above)
+  assignedAt DateTime @default(now())
+  assignedBy String
+
+  @@id([championId, bubbleId])
+}
+
 model Label {
   id          Int      @id @default(autoincrement())
   name        String   @unique
   description String?
```


